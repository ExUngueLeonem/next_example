import { FC } from 'react';
import styles from './Auth.module.scss';

const Auth: FC = () => {
    return (
        <div className={styles.foo}>
            Auth
        </div>
    );
};

export default Auth;